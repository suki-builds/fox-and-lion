# Copy this file to .env.local and fill in your own values.
# .env.local is gitignored — never commit real API tokens.

# DatoCMS > Settings > API tokens > "Read-only API token"
DATOCMS_API_TOKEN=

# Optional: DatoCMS > Settings > API tokens, used only for draft/preview mode
DATOCMS_DRAFT_API_TOKEN=

# A secret string you make up yourself, used to secure the /api/preview route
PREVIEW_SECRET=

# A secret string you make up yourself, checked against the X-Revalidate-Secret
# header on incoming requests to /api/revalidate (DatoCMS webhooks). See
# README.md for exact webhook setup. Generate one with:
#   openssl rand -hex 32
REVALIDATE_SECRET=
