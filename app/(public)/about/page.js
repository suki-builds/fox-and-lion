export const metadata = {
  title: 'About — Fox and Lion',
};

export default function AboutPage() {
  return (
    <div className="container" style={{ paddingTop: '2.5rem' }}>
      <h1>About Fox and Lion</h1>
      <p>
        Replace this with your real About copy from the current Wix site.
        This page is static (not pulled from DatoCMS) since it changes
        rarely — edit this file directly when you need to update it.
      </p>
    </div>
  );
}
